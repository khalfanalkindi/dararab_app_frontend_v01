"use client"

/**
 * Legacy shadcn Toaster — unused. Root layout mounts Sonner via `@/components/ui/sonner`.
 * Kept as a no-op so accidental imports do not break the build.
 */
export function Toaster() {
  return null
}
